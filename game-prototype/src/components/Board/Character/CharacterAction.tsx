import type { Component } from 'solid-js';

export const CharacterAction: Component = () => {
    return (
        <p class="text-sm">
            Action: <span class="font-semibold">None</span> on <span class="font-semibold">None</span>
        </p>
    );
};