---
import { Game } from "@components/Game";
export const testMode = process.argv.includes('--test');
console.log('Game in test mode:', testMode);
console.log(process.argv);
---

<Game client:only="solid-js" test={testMode} />