import { AddDialogue } from '@helpers/dialogueStore';

export function GamePresentation() {
    AddDialogue("Welcome to the game prototype!");
    AddDialogue("This is a simple game where you can choose characters and roll dice.");
    AddDialogue("Let's start by choosing your characters.");
    AddDialogue("----------------------------------------");
}